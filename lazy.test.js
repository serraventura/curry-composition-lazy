const Lazy = require("./lazy");

describe("Lazy", () => {
  it("add() - should add func", () => {
    expect(true).toBeTruthy();
  });
  it("evaluate() - should add func", () => {
    expect(true).toBeTruthy();
  });

});
